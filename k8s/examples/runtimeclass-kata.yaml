apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: kata
handler: kata

# Notes:
# - The `handler` must match your container runtime configuration (containerd).
# - Some Kata installs use a different handler name (e.g. kata-qemu). Adjust to match.
