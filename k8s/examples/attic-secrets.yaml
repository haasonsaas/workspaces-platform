apiVersion: v1
kind: Secret
metadata:
  name: attic-secrets
  namespace: workspaces-system
type: Opaque
stringData:
  # Generate with:
  #   openssl genrsa -traditional 4096 | base64 -w0
  token_rs256_secret_base64: "REPLACE_ME"

